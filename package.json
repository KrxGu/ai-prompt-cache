{
  "name": "ai-prompt-cache-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @krishgupta/ai-prompt-cache",
    "dev": "npm run dev -w demo-next"
  }
}
